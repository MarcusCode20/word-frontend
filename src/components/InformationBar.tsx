import { Box } from '@mui/material';

const InformationBar = () => {
    return (
        <Box
            sx={{
                margin: 0,
                width: '100%',
                height: '10%',
                maxHeight: '55px',
                backgroundColor: 'pink'
            }}
        />
    );
};

export default InformationBar;
